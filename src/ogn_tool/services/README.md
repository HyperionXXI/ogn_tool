# Services Boundary

This package is reserved for external orchestration surfaces only (API, CLI, integrations).

Rules:
- No business logic
- No RF computation
- No inference logic
- Orchestration adapters only
